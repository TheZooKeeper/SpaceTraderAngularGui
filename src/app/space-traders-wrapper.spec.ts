import { SpaceTradersWrapper } from './space-traders-wrapper';

describe('SpaceTradersWrapper', () => {
  it('should create an instance', () => {
    expect(new SpaceTradersWrapper()).toBeTruthy();
  });
});
